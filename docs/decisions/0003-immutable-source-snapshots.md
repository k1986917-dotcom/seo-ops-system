# ADR-0003：所有原始输入使用不可变快照

- 状态：Accepted
- 日期：2026-07-14

## 背景

旧流程在成功导入后会删除 raw 文件，并把完整 GSC 压缩为少量 Markdown Top-N，导致历史、维度和审计能力丢失。

## 决策

- 上传或路径导入时先复制原始字节并计算 SHA-256。
- 相同站点、来源类型、哈希只解析一次。
- 解析失败也保留快照和错误记录。
- 派生表可以重建，快照不可覆盖或由正常导入流程删除。

## 结果

任何机会都能回溯到实际输入；解析器升级后可以重新派生，不需要用户重新寻找历史导出。

