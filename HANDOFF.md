# Handoff — 当前接手状态

最后更新：2026-07-14（Asia/Shanghai）

## 一句话状态

首个可运行里程碑已经完成：网页、SQLite、真实 GSC/CMS 导入、首版机会分析与可选 AI 边界均已贯通。

## 已确认的业务目标

- 用户是单人网站运营者。
- 可执行权限主要是发布、修改、删除文章和产品页面，以及自行决定文章主题。
- 现有第一方输入是 GSC Excel、博客 JSON、产品 JSON。
- 系统输出不是“尽量多写文章”，而是每天/每周最值得做的少量动作及其证据。
- 人工保留发布、删除及风险内容的最终决定权。

## 当前实现范围

- [x] 新项目与文档体系
- [x] FastAPI 网页骨架
- [x] SQLite Schema 与默认站点
- [x] 不可变文件快照
- [x] GSC 标准/对比工作簿导入
- [x] CMS blogs/products JSON 导入
- [x] 首版机会引擎与 Top 3
- [x] AI Provider 适配接口
- [x] 自动测试与页面响应验收
- [ ] 浏览器视觉验收（当前桌面浏览器连接被沙箱故障阻断）

## 已核实的真实输入

旧项目只读路径：`/home/laoma/seo-workflow/laserpointerhub/raw`

- GSC 标准导出：图表、查询数、网页、国家_地区、设备、搜索结果呈现、过滤器。
- GSC 对比导出：过去 28 天与先前 28 天，查询和页面等维度均有对比列。
- Blog JSON：顶层 `type/exportedAt/count/excludedFields/items`，当前 65 条。
- Product JSON：相同顶层结构，当前 15 条。

禁止直接移动或清理这些旧文件。真实导入测试只复制到新项目的快照目录。

## 当前真实数据状态

- GSC 对比导出：2,220 条标准化指标。
- GSC 标准导出：922 条标准化指标。
- Blog：65 条。
- Product：15 条。
- 首轮分析：32 个候选，只选择 2 个进入组合；没有合格防损项时不凑满 Top 3。
- 当前最高价值项：更新 `/blog/best-laser-pointer-under-300` 前先核对站内相对 CTR。
- 当前补证据项：确认查询 `how to clean laser pointer` 的目标页面。
- 自动测试：7 passed。
- 本地页面：`/`、`/imports`、`/opportunities`、`/method`、`/ai` 与 CSS 均返回 200。

## 关键方法边界

- 第一版允许经验阈值，但必须明确标记为 cold-start heuristic。
- 优先做资格门槛，再做排序；高蚕食、无需求证据、不可执行的候选不能仅靠高分进入组合。
- 接受/拒绝不是效果反馈；必须记录执行前基线和后续观察。
- GSC 普通 Excel 的查询表与网页表不是联合维度。涉及具体查询归属时需要页面/查询筛选导出或 API 数据。
- 商业价值在没有订单数据时只能作为代理指标，不得显示为收入预测。

## 怎样运行

```bash
cd /home/laoma/seo-ops-system
source .venv/bin/activate
seo-ops
```

打开 `http://127.0.0.1:8787`。测试命令：`pytest`；质量检查：`ruff check src tests tools`。

## 下一位接手者的第一步

先运行测试并打开首页与机会页。下一步优先实现：

1. 页面或查询筛选后的 GSC 定向导入，建立真实 query→page 关系。
2. 已接受任务的执行记录和 7/28/56 天观察页面。
3. 内容相似/蚕食资格门槛，必须基于正文与意图而不是单词重合。
4. 再将合格机会转换为 research brief；不要先扩展 AI 长文写作。

## 已知风险

- 旧博客正文中存在历史编码异常字符，导入按 UTF-8 原样保留，不在导入阶段擅自清洗正文。
- 商品 JSON 没有完整 canonical URL，目前按 `/products/{slug}` 推导，推导值必须允许人工修正。
- 第一版尚未接入 GSC API，因此查询—页面联合数据只能作为定向补充输入。
- 当前 AI 未配置，disabled 模式已验证；尚未对真实供应商做集成测试。
- 内置浏览器连接因桌面沙箱初始化故障未完成截图验收，这不是网页服务启动错误。

