# 项目接手说明

本文件约束所有后续开发者和 AI 代理。开始工作前依次阅读：

1. `HANDOFF.md`
2. `docs/PROJECT_CONTEXT.md`
3. `docs/METHOD_GOVERNANCE.md`
4. 与当前任务有关的 ADR
5. `docs/WORKLOG.md` 最近一条记录

## 不可破坏的原则

- 原始导入文件是不可变审计材料：只复制、计算哈希、解析，永不覆盖或删除。
- 数据事实、程序推断、经验阈值和 AI 判断必须分开存储、分开展示。
- 没有查询—页面联合数据时，不得凭关键词重合声称某查询属于某页面。
- 没有订单/GA4 数据时，不得输出预期收入或查询级转化结论。
- AI 不得重新计算分数、绕过资格门槛、制造来源或自动发布。
- 所有作者、实测、案例和引用必须真实可追溯。
- 只推荐运营者有权限完成的动作；技术 SEO 问题可以记录，但不能冒充可执行任务。
- 每项新规则必须登记版本、证据等级、理由、失效条件和复查日期。

## 工作记录要求

每个有实质变化的工作单元必须：

1. 更新 `HANDOFF.md` 的状态与下一步。
2. 向 `docs/WORKLOG.md` 追加日期、完成内容、验证结果和遗留问题。
3. 架构或方法边界改变时新增/更新 ADR。
4. 用户可见功能变化时更新 `CHANGELOG.md`。
5. 运行与改动风险相称的测试，并记录实际命令和结果。

## 工程约定

- Python 3.12+，代码放在 `src/seo_ops`，测试放在 `tests`。
- Web：FastAPI + Jinja + 少量原生 JavaScript；不要在第一版引入前后端双工程。
- 数据：SQLite；Schema 变更必须可迁移，不能让用户删除数据库重来。
- 外部调用集中在 `services/`，不得散落在路由或模板中。
- 方法规则集中在 `rules/`，不得把业务阈值硬编码到 HTML。
- 所有时间以 UTC 存储、在界面按用户时区显示。
- API Key 只从环境变量或未来的加密凭据存储读取，绝不写入数据库、日志、HTML 或 Git。

## 验证标准

- `pytest` 全部通过。
- 导入同一文件两次不会重复写入指标或内容快照。
- 解析失败仍保留原始快照并显示可理解错误。
- 没配置 AI 时所有非 AI 功能可正常运行。
- 页面至少在 1280px 桌面与窄屏下无明显布局破坏。

